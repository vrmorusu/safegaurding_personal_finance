blend_ratio = 0.7 hybrid_returns = np.concatenate([ his-torical_pd["return"].sample(frac=blend_ratio, re-place=True, random_state=42), np.random.choice(mc_returns, size=int((1 - blend_ratio) * len(mc_returns)), replace=True) ]) hybrid_var = np.percentile(hybrid_returns, 5) 4. Stress Testing Stress testing evaluates the model under extreme conditions (Ba-sel III, 2011). stress_returns = np.concatenate([ hy-brid_returns, np.random.uniform(-0.2, -0.15, size=1000), np.random.uniform(0.15, 0.2, size=1000) ]) stress_var = np.percentile(stress_returns, 5)
