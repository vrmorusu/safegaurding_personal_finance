from pyspark.sql import SparkSession from pyspark.sql.functions import col, rand, stddev, mean, lit import pandas as pd import numpy as np import mat-plotlib.pyplot as plt import seaborn as sns from pyspark.sql import SparkSession import sys,os os.environ['PYSPARK_PYTHON'] = sys.executable os.environ['PYSPARK_DRIVER_PYTHON'] = sys.executable # Initialize Spark session spark = SparkSes-si-on.builder.appName("RiskModeling").master('local[*]').getOrCreate() import sys print (sys.path) # Load historical data (e.g., daily stock returns) data = [ ("2023-01-01", -0.02), ("2023-01-02", 0.01), ("2023-01-03", -0.015), ("2023-01-04", 0.03), ("2023-01-05", -0.01), ("2023-01-06", 0.02), ("2023-01-07", -0.005), ("2023-01-08", 0.015), ("2023-01-09", -0.03), ("2023-01-10", 0.025), # Add more rows or load a dataset from a file for realistic scenarios ] columns = ["date", "daily_return"] hist_df = spark.createDataFrame(data, columns)
